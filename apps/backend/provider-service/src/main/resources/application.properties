# Enable graceful shutdown so the server stops accepting new requests and waits for active ones to complete
server.shutdown=graceful
# Maximum time to wait for ongoing requests to finish during shutdown
spring.lifecycle.timeout-per-shutdown-phase=30s

# Optional: reduce default keep-alive for connectors if needed (commented out)
# server.connection-timeout=30s
